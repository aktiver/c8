# Build status — MCP Agent 0.6.0

The cumulative checksum, JSON, SQL boundary, authentication, long-input, managed-agent and user-tool static suites pass. Native Cargo/Clippy tests, Helm rendering, live RLS, MCP conformance, malicious-server, DNS-rebinding, credential-rotation, side-effect approval, HA and Kubernetes autoscaling tests remain required because their toolchains and infrastructure are not available in this workspace.
